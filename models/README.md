# Models

This folder contains the PPO checkpoint used for the included AccidentEnv evaluation result.

## Included checkpoint

- `ppo_accident_env_final.zip`: PPO checkpoint trained on `AccidentEnv` with `LidarObservation`.

## Not included

Intermediate checkpoints, TensorBoard logs, monitor logs, and temporary training outputs are excluded from the public repo.
